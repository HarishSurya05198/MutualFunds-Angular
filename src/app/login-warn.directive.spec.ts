import { LoginWarnDirective } from './login-warn.directive';

describe('LoginWarnDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginWarnDirective();
    expect(directive).toBeTruthy();
  });
});
